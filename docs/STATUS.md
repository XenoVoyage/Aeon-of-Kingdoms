# Project status

This is the current-state handoff and the sole owner of the current phase, evidence, deployment, and engineering-standard adoption state. Active redesign phases and acceptance gates belong in [`REDESIGN.md`](REDESIGN.md). Enduring contributor rules remain in `AGENTS.md`, and published history remains in `CHANGELOG.md`, at the repository root; neither file is part of the minimal Pages payload.

## Current truth

| Area | Status |
| --- | --- |
| Canonical source version | `v2026.8.22j` in `VERSION.txt`; Phase 6 is closed, the Phase 7 implementation candidate is deployed on `main` at `461c100eb48d83d2ba960a300313c19848d89f41`, and this evidence follow-up corrects the Pages artifact's hidden-file boundary before Phase 7 can close; intentionally untagged, with no GitHub Release authorized |
| Historical runtime | `v2026.8.15` was observed playable at commit `7f88655` on 2026-08-15, but the product owner rejected its UI, art, map, gameplay feel, terminology, and AI as the future direction; its misleading public tag and GitHub Release were retired on 2026-08-21 while the commit and normal Git history remained reachable |
| Active phase | Phase 0 through Phase 6 are approved and closed. Phase 7 is active under the frozen product-hardening contract in [`PHASE7_PRODUCT_HARDENING.md`](PHASE7_PRODUCT_HARDENING.md); its bounded implementation candidate is published and its available cloud-Chrome evidence is recorded, but unavailable evidence, material decisions, root promotion, and closure are not claimed |
| Redesign gameplay implementation | The approved deployed Phase 6 foundation owns the unchanged fixed 20 Hz nine-command battle rules plus one detached no-cheating Standard planner, three bounded task forces, legal-command local-skirmish orchestration, mixed replay, strict composite checkpoints, and accessible You/Computer identity. The separate Phase 7 candidate adds presentation-only suspension, browser-feature settlement, focus/semantic/reflow hardening, bounded soak evidence, and cleanup. Modes, campaign, rooms, signaling, networking, audio, and storage remain absent |
| Current source boundary | Minimal redesign status page; mood, approved Phase 1A and Phase 1B review surfaces; repository-only masters; approved Phase 2 through Phase 6 routes; the bounded 24-file Phase 5 route and damage package; the closed six-file Phase 6 local-skirmish route; the frozen Phase 7 contract; and exactly four candidate files under `phase7/` |
| GitHub Pages | Phase 7 pull request `#30` passed audit `32598168338` and squash-merged as `461c100eb48d83d2ba960a300313c19848d89f41` with candidate tree `1c2240fc097893c2099a7eb19602a641fa5a093d`. Main audit `32598490594` and Pages run `32598490580` succeeded. Artifact `9482284342` contained all 166 public files byte-identical to the merged stage, but the action's default omitted the staged empty `.nojekyll`; this follow-up enables the pinned v5 action's bounded hidden-file input before exact 167-file artifact identity is claimed |
| Multiplayer | Not shipped and not part of Phase 6. The owner selected a later private two-player host/client room joined by a short room code; GitHub Pages remains only the static client host, so reviewed signaling and likely TURN still belong to the networking phase |
| Engineering standard | v1.0 governs the repository and is structurally applied, but remains `adopting`; structure, ownership, dependencies, security, assets/provenance, CI, Pages delivery, documentation, and authorized prototype-marker retirement were audited, while unresolved applicable evidence prevents a `verified` claim |
| Verification | Phase 7 passes 19/19 focused and 281/281 complete dependency-free checks, including byte locks for closed Phase 2 through Phase 6 source/evidence, exact viewport and memory arithmetic, reduced-motion authoritative equivalence, a 12,000-active-tick soak, reachable cap boundaries, an 8,192-command replay, checkpoint convergence, ownership-hue suppression, and an executable real-app VM journey covering keyboard control, production/cancellation, lifecycle and viewport gates, failure, completion, and teardown. Exact staging produces 166 public files plus `.nojekyll` (167 total). Protected publication and an actual 1363×936 DPR-1 cloud-Chrome Standard/Compact journey passed, including computed overflow/target checks, native sequential focus, dialog focus containment, keyboard production/cancellation, pause, Navigation, movement, fullscreen-denial recovery, menu teardown, and a clean Pages-origin console. The corrected 167-file artifact, direct live-byte comparison, other actual viewports/browser families, physical devices, actual 200% browser zoom, thermal observations, product decisions, root promotion, and Phase 7 closure remain pending |

Phase 0's product-truth gate is complete: the rejected runtime is historical and its public release markers are retired. Remaining broad keyboard, physical-device gallery, generalized pre-existing-cache, and rollback observations stay recorded as operational evidence debt. The current Pages build contains the approved bounded Phase 6 local-skirmish foundation, frozen Phase 7 contract, and published Phase 7 candidate. Recorded workflows and live browser observations establish only the named deployment, resources, desktop viewport, and journeys. Source automation does not fabricate physical-device landscape quality, complete cache migration, rollback quality, Phase 7 closure, or later-phase evidence; the owner-supplied capture establishes only the visible portrait mobile state described below.

## Approval record

On 2026-08-15, the product owner approved Phase 0 and authorized its implementation. That approval locks the phased process and permits the truth-and-cleanup transition; it does not approve Phase 1 visuals, later gameplay implementation, a tag, or publication without their own evidence and gates.

On 2026-08-20, the product owner reviewed the published concept set. The battlefield, faction contrast, combat language, and interface were accepted as mood direction. The literal entity detail and realism were rejected as the production target because they do not prove readability on compact screens, consistency across crowded battles, affordable animation, or a sustainable six-faction scope. The owner approved the smaller Phase 1A production-feasibility brief and authorized replacement references, but did not approve final art or gameplay implementation.

The deployed `v2026.8.20a` proof implements that brief as a review surface: one crowded battlefield, compact desktop and phone frames, two three-role faction lineups, the exact three structure categories, a six-layer map diagram, a four-family animation pose board, and non-color-only state cues. Source completeness and deployment are not owner approval. No full roster, shipping sprite, gameplay renderer, implementation phase, tag, or release is authorized by its existence.

The owner then rejected that proof as an approval surface because it mixed detailed painted targets with geometric structure, map, and animation diagrams, leaving the intended visible game unclear. The owner authorized a direct replacement that moves further toward cartoon-leaning painted 2D, shows one coherent battlefield/entity/structure language, simplifies representative entities, and proves a practical animation method.

During review of the local replacement, the owner identified misassembled limbs, an incorrect bone overlay, unclear attack direction, undersized weapons, and unstable idle/movement roots. The owner accepted the environment-only battlefield, three structures, damage progression, and player-color boundary. After further correction, the owner approved the production method recorded in [`PRODUCTION_ART.md`](PRODUCTION_ART.md): coherent baked full-body frames; canonical right-facing art and an exact X-mirrored left facing; one stable idle frame; four movement frames whose upper body and equipment remain invariant; six-frame action and defeat sequences; and separate frame-aligned player-color masks. The Astral Guardian established this method.

On 2026-08-21 the same method was applied to the Gravebound Reaver, Starbow, Hollow String, Aegis Titan, and Ossuary Colossus. The complete direct package includes six atlases and masks, actual-scale state playback, neutral shared structures and faction headquarters with bounded ownership masks, six player colors plus six symbols, the accepted damage progression, and separate desktop/phone battlefield compositions. The owner first found the set ready except that Aegis Titan's body direction disagreed with its gait and punch. The feature-branch candidate replaced those two states with coherent canonical-right art and exact mirrored-left playback. After directly reviewing the corrected Aegis Titan and integrated set, the owner approved both on 2026-08-21. The owner then authorized the standardization and Phase 1A closure change to merge and deploy through pull request `#10`. Phase 1A is therefore closed as a non-playable production-feasibility and visual-direction proof. The bounded approved review later published through pull requests `#11` and `#12`, while the excluded direct masters remained repository-only. The rejected prototype's misleading public tag and GitHub Release were retired without rewriting history.

The complete Phase 1B candidate later assembled the owner-retained menu, map-dominant HUD, full two-faction identity language, battlefield, landscape controls, viewport policy, and measured runtime-art envelope. On 2026-08-21 the owner explicitly approved that complete candidate and authorized Phase 2 to begin. The owner then pointed out the lower-body omission in three Astral Guardian movement cells in the derived browser samples; inspection confirmed the approved authoring master was intact, and the bounded derivatives were corrected from that source. Phase 1B remains closed.

On 2026-08-22 the owner explicitly approved the complete Phase 2 candidate, said it looks good, and authorized Phase 3. This closes the empty-battlefield/camera gate while retaining unobserved compact, broad-browser, and physical-device evidence as debt. The same owner message selected a simple future multiplayer experience: one player hosts a private two-player room and the other joins with a short room code. The room-code choice is recorded now, but networking remains deferred; GitHub Pages cannot supply signaling or TURN and Phase 3 retains `connect-src 'none'`.

On 2026-08-22 the owner reviewed the repaired deployed Phase 3 candidate, explicitly approved the phase, said it looks really good, and authorized the next phase. This closes the gate that selected armies move naturally without stacking, visual drift, or placeholder ships. Source completion, automation, named rendered observations, protected publication, owner approval, and unobserved device evidence remain distinct categories. Phase 4 is authorized; its exact structures/economy/production/rally contract must be frozen before implementation, and later combat, AI, networking, tags, and GitHub Releases are not authorized by this approval.

The owner subsequently clarified a standing goal-mode authorization: complete each remaining phase autonomously, perform the full technical and visual audit, publish through the protected pull-request path, merge verified work, confirm GitHub Pages, and proceed immediately to the next phase. This authorization permits the agent to close a phase after its exact gate and available evidence pass without requesting routine approval again. It does not permit fabricated observations, concealed evidence debt, unreviewed external infrastructure, destructive history rewriting, a Git tag, or a GitHub Release.

Under that standing authorization, Phase 4 closed on 2026-08-22 after 45/45 focused and 167/167 complete checks, reproducible structure assets, protected pull requests `#22` through `#24`, successful final main audit and Pages deployment, exact live-byte comparisons, and named desktop rendered journeys all passed. This is an evidence-backed autonomous closure, not a fabricated claim of a separate explicit owner review. The remaining device/browser rows stay debt, and Phase 5 is authorized only inside the exact combat/tactical-command contract frozen in [`PHASE5_COMBAT_TACTICS.md`](PHASE5_COMBAT_TACTICS.md).

The Phase 5 implementation candidate was assembled under that authorization on 2026-08-22 and passes 57/57 focused and 225/225 complete dependency-free checks. Protected pull request `#26`, the corrected exact remote tree, successful main audit and Pages deployment, complete 24-file live-byte comparison, clean Pages-origin console, and named Standard/Compact desktop combat journeys then passed. Phase 5 therefore closed under the standing authorization, with unavailable device/browser rows retained as debt rather than fabricated. Phase 6 is authorized only inside the exact contract in [`PHASE6_STRATEGIC_AI.md`](PHASE6_STRATEGIC_AI.md).

The Phase 6 implementation was assembled and closed under that authorization on 2026-08-22. It reuses the exact closed Phase 5 battle modules and adds only the detached planner, local-skirmish orchestration/checkpoint wrapper, minimal You/Computer shell changes, and focused verification. Protected pull request `#28`, exact-tree merge, successful main automation and Pages deployment, all 161 allowlisted live-byte comparisons, named Standard/Compact desktop journeys, the active-pressure defense/recovery/regroup journey, exact tick-3,715 passive outcome, pause/menu recovery, and the clean Pages-origin console passed. Phase 7 is now active only under its frozen product-hardening contract; unavailable physical-device and broad-browser evidence remains debt rather than a fabricated Phase 6 result.

The Phase 7 implementation candidate was assembled under the same standing authorization on 2026-08-22. It adds exactly four local route files over byte-locked Phase 2 through Phase 6 owners and changes no authoritative battle or AI rule. The focused source suite passes 19/19, the complete dependency-free suite passes 281/281, and exact staging adds only those four public files. Pull request `#30`, its audit, squash merge, main audit, Pages deployment, and an actual 1363×936 DPR-1 cloud-Chrome review then passed. Native focus order and dialog containment, Standard and Compact art tiers, production/cancellation, synchronous pause, Navigation, movement, fullscreen-denial recovery, menu teardown, computed target/overflow checks, and clean Pages-origin console output were observed. The deployment artifact matched all 166 public files byte for byte but omitted `.nojekyll` under the upload action's hidden-file default; the bounded workflow correction is therefore part of this evidence follow-up. This is not closure: unavailable browser/viewport/physical-device rows, exact corrected-artifact and direct live-byte evidence, material product decisions, and root promotion remain separately pending.

## Engineering Standard v1.0 adoption

The existing-project cleanup standard supplied by the owner governs this repository. The companion new-project standard was used as a compatible baseline, not copied into the repository as a competing instruction source.

| Disposition | Evidence |
| --- | --- |
| Completed in this pass | Added the exact standard markers; consolidated instruction and status ownership; recorded exact run, test, staging, and diff commands plus definition of done; audited tracked files, dependencies, secrets, local/external resources, CSP, service worker, Actions, Pages staging, assets, provenance, licenses, branch/PR/issue state, and repository settings visible through GitHub; removed six proven-dead feasibility assets totaling 639,220 bytes; updated the pull-request template and consistency tests; merged protected pull requests `#10` through `#30`; verified the successive Phase 1A through Phase 7 source/staging boundaries; reproduced the approved Phase 4 and Phase 5 checks and exact structure derivatives; completed Phase 5 and Phase 6 protected publication, exact remote-tree and live-byte verification, page-origin console review, and named Standard/Compact desktop journeys; corrected the earlier cache-sensitive stylesheet URL, Phase 3 initial-paint race, Phase 4 missing map dependency, Phase 4 paused-command race, and caught the pre-merge large-blob upload truncation; froze and assembled the bounded Phase 7 candidate, its 19-check focused suite, expanded real-app VM journey, 281-check complete pass, protected publication, and available real cloud-Chrome semantic/visual/interaction review; identified and corrected the Pages artifact's default `.nojekyll` omission; and verified retirement of the rejected tag and Release while preserving commit history |
| Deferred or unavailable | Phase 7 corrected-artifact/direct-live-byte evidence, remaining browser/physical evidence, material decisions, root promotion, and closure; physical-device landscape and broad browser-matrix observations; tablet/phone/aspect/portrait Phase 3–7 rendering, actual 200% browser zoom, touch-device behavior, reduced-motion preference emulation, and display-cutout emulation; exact cloud-browser version and Resource Timing data where an inspection surface withholds them; generalized stale-cache and rollback observations beyond corrected cases; protection/ruleset settings that the GitHub connection could not expose; keyboard-only spatial destination control beyond the existing keyboard camera/control path; and a live blocked-spawn recovery journey |
| Not applicable now | Package installation, dependency lockfile and dependency advisory scan because the repository has zero runtime/test packages; account, payment, analytics, telemetry, server, database, personal-data migration, and scientific/data-reproducibility gates because none exists in the active static source boundary |

The rejected prototype source remains intentionally preserved for historical regression coverage in this pass. Removing that larger archive is a separate consequential cleanup, not a hidden part of standard adoption.

## Owner-locked redesign requirements

- Original design made for Aeon of Kingdoms; Neon Voyage is only an example of restraint and simplicity.
- Landscape-only gameplay with pan, zoom, and a rotate-device gate in portrait.
- Illustrated map terrain with meaningful impassable mountains, cliffs, structures, and routes.
- `Entity` is the authoritative code term; faction-specific names remain player-facing.
- Exactly three structure categories: faction-unique headquarters, shared Resource Points, and shared Production Outposts.
- Cartoon-leaning painted 2D tactical miniatures with broad silhouettes and restrained detail, proven at actual desktop and phone gameplay scale before roster expansion.
- Data-driven map layers for ground, decoration, navigation/blockers, anchors, dynamic entities, and foreground occlusion; visual pixels never determine walkability.
- Four authored animation families under [`PRODUCTION_ART.md`](PRODUCTION_ART.md): one-frame idle, four-frame lower-body gait, six-frame attack/cast, and six-frame defeat; canonical right facing, exact X-mirrored left facing, shared root, and simulation-owned contact timing.
- Separate frame-aligned player-color masks on all player-controlled entities and ownable structures, with color plus non-color ownership cues for up to six players.
- Producing structures use authoritative queues, visible progress bars, spawn validation, and rally points.
- Combat supports explicit entity targeting and autonomous nearby engagement with readable states.
- Movement is slower, grouped, formation-aware, and non-stacking.
- AI plans production, defense, objectives, task forces, and attack timing rather than ordering one global pile.
- Human, AI, replay, campaign, and future networking share one deterministic command boundary.

## Evidence boundary

The published menu, battlefield, faction, structure, combat, mobile-control, and production/rally frames remain available in the [mood-reference gallery](../concepts/). The [published Phase 1A review](../concepts/feasibility/) presents the approved production direction reflected in the GDD: all six representatives and their actual-scale state playback, ownership, environment, damage, atlas audit, and desktop/phone compositions. The exact method is defined in [`PRODUCTION_ART.md`](PRODUCTION_ART.md), and repository-only authoring masters remain under `concepts/feasibility/phase1a/`. Its 37 staged visual assets are communication and asset-review evidence, not gameplay screenshots or implementation evidence. The [approved Phase 1B review](../concepts/phase1b/) and [`PHASE1B_VISUAL_LOCK.md`](PHASE1B_VISUAL_LOCK.md) lock the menu/HUD, complete twelve-identity language, battlefield, landscape controls, viewport matrix, and measured local asset envelope without adding executable gameplay.

The approved Phase 2 source is the first replacement implementation foundation: a non-authoritative semantic shell, bounded camera, six-owner map schema, environment renderer, explicit blockers/anchors, and debug presentation under [`PHASE2_FOUNDATION.md`](PHASE2_FOUNDATION.md). The approved deployed Phase 3 foundation builds on it with the bounded entity, selection, movement, replay, and presentation behavior in [`PHASE3_ENTITY_MOVEMENT.md`](PHASE3_ENTITY_MOVEMENT.md). The approved deployed Phase 4 foundation adds the exact structures/economy/production/rally behavior in [`PHASE4_STRUCTURES_ECONOMY.md`](PHASE4_STRUCTURES_ECONOMY.md). The approved deployed Phase 5 foundation adds combat and tactical commands under [`PHASE5_COMBAT_TACTICS.md`](PHASE5_COMBAT_TACTICS.md). The approved deployed Phase 6 foundation adds the bounded no-cheating strategic AI and local skirmish under [`PHASE6_STRATEGIC_AI.md`](PHASE6_STRATEGIC_AI.md). [`PHASE7_PRODUCT_HARDENING.md`](PHASE7_PRODUCT_HARDENING.md) owns the active frozen product-hardening implementation and evidence gate. The separate four-file candidate is an implementation claim only for that bounded presentation layer; source, staging, Actions, HTTP, named rendered-browser evidence, and owner authorization remain separate facts, and none establishes an unobserved publication, viewport, physical-device result, root promotion, or Phase 7 closure.

### v2026.8.20 deployment observation

On 2026-08-20, protected pull request `#6` merged as `919cc933a4def3a6688208f3e5a2180cc4d4687e`. Main audit run `32347611623` and Pages run `32347611618` completed successfully. A cloud desktop browser then loaded the canonical status and `concepts/` routes, observed the `v2026.8.20` label and revised Phase 1 boundary, and loaded all eight local WebP references at their recorded natural dimensions. No warning or error from the Pages origin appeared in that session. Browser-extension diagnostics were excluded because they are not emitted by the site. This observation does not establish pixel-level visual approval, responsive or keyboard behavior, a pre-existing-cache upgrade, another browser/device, or a physical-device result.

### v2026.8.20a deployment observation

On 2026-08-20, protected pull request `#8` merged as `75ec47c2bca9ea325f5b9508c06d44f3eb1aff1c`. Main audit run `32351430376` and Pages run `32351430306` completed successfully. That frozen source passed 68/68 integrated checks and staged the exact 24-file Pages allowlist. The canonical status route and `concepts/feasibility/` returned HTTP 200. SHA-256 checks confirmed that the live status entry, proof entry, proof stylesheet, three WebP targets, and three SVG diagrams match merged source byte for byte. A cloud desktop browser at 1363×936 loaded the `v2026.8.20a` status, Phase 0 complete state, Phase 1A in-review state, and direct proof route. The proof page's full rendered length, compact-phone layout, keyboard flow, page-origin console, stale-cache migration, and physical-device readability remain unobserved; exact bytes do not establish those qualities or owner approval.

### v2026.8.21 deployment observation

On 2026-08-21, protected pull request `#10` squash-merged as `0d74dd9174f0db873c1c9ea8cfc824c1ea231660`; its origin branch was automatically deleted. Main audit run `32496788387` and Pages run `32496788440` completed successfully on Node.js 20.20.2 with 72/72 integrated checks. Pages staged the exact 31-file allowlist plus `.nojekyll`. Direct samples of the canonical live index, status, redesign, and production-art documents matched merged source. Direct requests beneath `concepts/feasibility/phase1a/` returned HTTP 404, as required by the unchanged allowlist boundary. This establishes merge, deployment, exact sampled content, and intentional package exclusion—not a tag, release, rendered-browser review, physical-device result, or redesigned gameplay.

### v2026.8.21a publication and retirement evidence

The owner authorized replacing the public superseded v5 proof with the optimized approved Phase 1A review, retaining the main-menu design, and retiring the rejected prototype's `v2026.8.15` public tag and GitHub Release without rewriting history. Protected pull request `#11` squash-merged as `b68dad6c611e9885967d866b776af38c776acd75`; main audit run `32508895280` and Pages run `32508895290` completed successfully. A warm-cache browser session then revealed that old proof CSS could outlive the new HTML. Pull request `#12` added the versioned stylesheet reference and squash-merged as `d6ca16927e9dfef4551323d66f6d96930e6e2f38`; main audit run `32509285741` and Pages run `32509285737` completed successfully.

The final source passes 72/72 integrated checks and `git diff --check`, and stages 57 allowlisted files plus `.nojekyll`: 37 approved review assets totaling 2,503,564 bytes are included, while all 11 v5 assets totaling 2,263,262 bytes are excluded. Raw atlases, masks, structure masters, and metadata remain repository-only. A cloud browser loaded the deployed review with the new title and heading, all 31 images, six closed-by-default playback disclosures, and all 24 authored-scale state links. Opening and closing a playback disclosure worked; after layout settled, client and scroll width both measured 1348 pixels; no page-origin console warning or error was observed. A direct no-cache request to the canonical route returned the current HTML and its versioned `proof.css?v=2026.8.21a` reference.

Remote Git and the public GitHub API then reported no `refs/tags/v2026.8.15` ref and no Release for that tag, while commit `7f88655f10504f44496fbba2e17871b16a5fe115` still returned successfully and remained an ancestor of `main`. This establishes retirement without rewriting history. It does not establish a physical-device landscape result, a complete browser/device matrix, generalized stale-cache migration, rollback quality, or gameplay.

### v2026.8.21b original Phase 1B candidate evidence

The script-free candidate at `concepts/phase1b/` presents the owner-retained menu, one map-dominant HUD hierarchy, the approved environment/battlefield language, exactly three structure categories, six authored world layers, all twelve proposed permanent opening identities, desktop/tablet/phone landscape interactions, portrait rotate gate, viewport/browser matrix, and the local runtime-art envelope owned by [`PHASE1B_VISUAL_LOCK.md`](PHASE1B_VISUAL_LOCK.md). It reuses only the retained menu and approved Phase 1A visual evidence; no rejected prototype, mixed proof, v5 art, generated rig, independent left/right art, fourth structure, color-only ownership, or Neon Voyage visual source enters the candidate.

The runtime-envelope audit cropped and resized every unique frame independently before packing, re-locked movement upper bodies, clamped masks to base alpha, and lossless-WebP round-tripped the result with ImageMagick 6.9.12-98 Q16 and libwebp 1.3.2. The original six-pair survey measured 431,220 bytes at 96 and 688,988 bytes at 128; doubling complete representative sets projected 862,440 and 1,377,976 bytes for twelve identities, while exact decoded RGBA arithmetic was 13.5 MiB or 24 MiB when only one tier was resident. Four Astral Guardian files totaling 183,574 bytes were staged as bounded browser-decode evidence for both tiers; they were not a complete roster or shipping runtime set. The checks proved upper-region identity, mask containment, and lossless round-trip, but did not detect the later owner-observed missing lower-body pixels in movement cells one through three of those derived Astral files.

Protected pull request `#14` squash-merged the candidate as `618d3498c302e08068be99eb7aa585a9a3d162d8`. Offline audit run `32516391298` and Pages run `32516391299` completed successfully. The merged source passes the focused Phase 1B suite at 5/5, the complete dependency-free suite at 77/77, `git diff --check`, and exact staging at 64 allowlisted files plus `.nojekyll`.

A live cloud Chrome review at a 1348×936 CSS-pixel viewport loaded the canonical `v2026.8.21b` status and Phase 1B routes. The 13,258-pixel review page had equal 1,348-pixel client and scroll widths. The owner-gate copy, retained menu, annotated battlefield, twelve identity contracts, desktop/tablet/phone/portrait compositions, runtime budget, and provenance sections rendered coherently. All ten local images completed with their recorded natural dimensions. In particular, the 96 base/mask pair reported 384×384 each and the 128 pair reported 512×512 each, and their visible pixels rendered in the atlas review. No page-origin console warning or error was observed; browser-extension metadata errors were excluded because they did not originate from the project page.

The browser connection identified itself only as Chrome and did not expose an exact version or Resource Timing API to the read-only inspection surface. It also did not expose viewport resizing, so the four embedded compositions were visually reviewed at the cloud desktop viewport while actual compact-page reflow, 200% rendered zoom, display-cutout emulation, and physical devices remain pending rather than fabricated. Implemented loading/fallback behavior, browser/GPU residency and resource release, layered runtime-map budgets, complete structure damage-state bytes, and gameplay remained unobserved. The product owner subsequently supplied the separate approval and correction evidence below.

### v2026.8.21c approval, sample correction, and Phase 2 source candidate

On 2026-08-21 the product owner explicitly approved the complete Phase 1B candidate and authorized Phase 2 to begin. This closes the menu/HUD, battlefield, twelve identities, landscape controls, viewport policy, and measured runtime-art envelope as the visual/interaction target. It does not approve the Phase 2 implementation or authorize Phase 3, a tag, or a GitHub Release.

The owner then identified missing legs in movement cells one through three of the derived Astral Guardian browser samples. Direct inspection established that the approved 384-pixel authoring atlas and aligned mask contained the complete lower bodies. The four bounded derivatives were repaired with Sharp 0.35.3, libvips 8.18.3, and libwebp 1.6.0 by preserving the existing upper regions and restoring only the lower regions from independently resized approved source cells, then applying the aligned-mask repair and alpha clamp. The corrected 96 base/mask pair totals 74,152 bytes; the 128 pair totals 118,396 bytes; all four total 192,548 bytes. Exact current hashes and provenance are recorded in [`ASSETS.md`](ASSETS.md). The protected merge and live review below verified those four exact deployed files and visibly complete movement poses.

The `v2026.8.21c` source candidate begins the Phase 2 contract in [`PHASE2_FOUNDATION.md`](PHASE2_FOUNDATION.md): a new semantic menu/battlefield shell, approved landscape viewport gate, bounded focus-centred camera, one two-player six-owner map definition, environment-only terrain drawing, explicit blockers/anchors, and a non-color-only navigation debug view. It deliberately omits Phase 3 entities and movement and all later simulation systems. The frozen source passes 4/4 Phase 1A, 18/18 combined Phase 1B/Phase 2 focused, and 90/90 integrated checks; stages exactly 72 public files plus `.nojekyll`; and passes diff hygiene.

Protected pull request `#16` squash-merged the candidate as `d17e8c9b4cc00a4beebf03aea443fd514197d608`. Pull-request audit run `32526124805`, main audit run `32526203611`, and Pages run `32526203607` completed successfully. Direct SHA-256 comparison found exact matches for all seven deployed Phase 2 files and the four corrected Astral Guardian browser samples.

A live cloud Chrome session at 1363×936 loaded the unchanged approved menu and the direct Phase 2 route without horizontal overflow. Begin opened the environment-only terrain on six ordered canvas surfaces, each 1363×936 in CSS and backing pixels in that session. Home reported camera `(836, 471)` at `1.00×`; zoom-in reported `1.18×`; Arrow Right moved the bounded camera to `(867, 471)`. Navigation debug exposed patterned and labelled blocker polygons; pause/resume updated the readable status; Menu returned focus to Begin; and direct `?view=battlefield&debug=1` entry focused the application with debug active. No warning or error from the Pages origin was observed; browser-extension metadata errors were excluded. The same session rendered all four repaired Astral samples at their exact 384×384 and 512×512 natural dimensions with visibly complete lower bodies. The browser surface did not expose an exact Chrome version or viewport resizing, so compact/tablet/phone/aspect/portrait live reflow, 200% zoom, broad browsers, and physical devices remained pending rather than fabricated. Subjective camera feel was pending in that session and passed only through the separate owner approval below.

### v2026.8.22 Phase 2 closure and Phase 3 planning candidate

On 2026-08-22 the product owner explicitly approved the complete Phase 2 candidate and authorized Phase 3. The same message selected the future private two-player host/client room-code experience. This source records both decisions, closes only the Phase 2 gate, and introduces [`PHASE3_ENTITY_MOVEMENT.md`](PHASE3_ENTITY_MOVEMENT.md) as the bounded next contract. It adds no Phase 3 runtime or networking code.

The local planning candidate passes 90/90 integrated checks, exact staging of 73 allowlisted public files plus `.nojekyll`, local Markdown and Pages-subpath reference checks, and `git diff --check`. Protected pull-request automation, merge, Pages deployment, and live-byte/browser evidence remain pending and must be recorded after they are observed. No tag or GitHub Release is authorized.

Protected pull request `#18` subsequently merged that planning baseline as `3051c50544e6fc595e632977a8096d4a079e20bc`. Main Offline audit run `32563896418` and Pages run `32563896375` succeeded, and sampled live status, Phase 1A review, Phase 1B review, and Phase 3 contract bytes matched merged source. This establishes publication of the planning boundary, not Phase 3 implementation or approval.

### v2026.8.22a Phase 3 implementation candidate

The deployed `main` source implements the bounded Phase 3 candidate without starting Phase 4. Its fixed 20 Hz integer simulation owns authoritative timing and outcomes; selection, camera, animation, and interpolation remain local presentation. The only authoritative player command is `MOVE`. Canonical replay logs, bounded snapshots, restore-and-continue behavior, and `fnv1a64` checksums exercise the same command boundary intended for future private host/client rooms, but no room, transport, signaling, TURN, or other network code exists.

The candidate exports all six approved representatives into 24 local lossless WebPs. Standard totals **734,126 encoded bytes** and retains at most **12 MiB** of decoded entity sheets; Compact totals **459,446 encoded bytes** and retains at most **6.75 MiB**; both tiers total **1,193,572 encoded bytes**. The loader fixes Astral Concord to seat 1 and Gravebound Court to seat 2, prepares one final owner-colored sheet per entity, retains that sheet plus the diagnostic base, and releases the decoded mask. The earlier 694,040- and 435,142-byte six-entity survey outputs were defective unpublished feasibility exports with blank lower-body rows; the four separately repaired published Astral Guardian Phase 1B samples remain untouched.

The frozen source passes 32/32 focused Phase 3 checks and 122/122 complete local checks with the recorded ImageMagick toolchain. A runner without that optional authoring executable reports one explicit regeneration skip while every committed atlas dimension, byte count, SHA-256, lossless encoding, manifest value, and decoded invariant remains checked. `git diff --check` passes, and the exact Pages allowlist stages 108 public files plus `.nojekyll` (109 files total). Independent audits found no authoritative-simulation, determinism, retained-memory, CSP, delivery, scope, or merge blocker; the arrival-marker presentation defect was corrected before freeze. Protected pull request `#19` squash-merged the implementation as `95c0c11c5f380c5582f737ab2a15831f350958f3`; pull-request audit `32566598007`, main audit `32566654447`, and Pages `32566654450` succeeded.

The first deployed 1363×936 cloud-Chrome review found a real initial-paint race: authoritative ticks advanced and art reported ready, but all six 1363×936 canvas backing stores remained transparent until Navigation requested a later full redraw. The shared renderer redundantly gated post-load drawing on a transient `HTMLImageElement.complete` value after resize had cleared the canvases. Pull request `#20` added a regression with a dimension-valid `complete: false` image, removed that redundant gate, and squash-merged as `bbd43f6de725a15733997500e592ade91878f93c`; pull-request audit `32567114056`, main audit `32567157738`, and Pages `32567157783` succeeded.

No-cache SHA-256 comparisons then matched merged source for the live root index, shared renderer, and Phase 3 app. Fresh Standard and Compact direct starts painted the terrain, anchors, foreground, ownership cues, and all twelve full-body entities before any redraw control. The same desktop session exercised single and five-entity selection, accepted and unreachable orders, spaced group movement, right- and exact mirrored-left presentation, successful teal `ARRIVED` feedback, pause/resume, zoom/Home, and menu recovery. No Pages-origin warning or error was observed; cloud-browser extension metadata errors were excluded. This evidence does not establish tablet/phone landscape, portrait gating, touch gestures, a keyboard-only entity command journey, another browser, or a physical device. The owner separately approved the complete Phase 3 candidate on 2026-08-22 and authorized Phase 4. No tag or GitHub Release is authorized.

### v2026.8.22c Phase 4 closure evidence

Implementation pull request `#22` merged as `fbd4852c81a584b188dcf90d4d0ced6ce7f4b6c0`. Its first live review exposed a missing Phase 2 map dependency that blocked startup; repair pull request `#23` merged as `e2d55ec0520dd236dda566ff9512932d79afbbea`. The continued journey then exposed a throttled-frame race that could briefly leave commands enabled after Pause; repair pull request `#24` merged as the final Phase 4 commit `10b510ba2dc9da19fef85b1cb78e4047a69c5240`. Final main Offline audit `32573016677` and Pages deployment `32573016684` succeeded.

The final source passes 45/45 focused and 167/167 complete dependency-free checks. Its exporter reproduces eight exact-RGBA lossless structure WebPs totaling 634,642 bytes, and the candidate stages 129 allowlisted files plus `.nojekyll` (130 total). No-cache SHA-256 comparisons matched the live root, Phase 4 entry, application, configuration, map, structure manifest, and all eight structure WebPs to merged source.

Fresh cloud-Chrome journeys at a 1363×936 desktop viewport loaded both Standard 128-pixel and Compact 96-pixel art tiers, all six landscape layers, all three structure categories, neutral and color-plus-symbol owned cues, full-body entities, and exact ground-root ordering. The named primary journey covered army selection and movement, a completed Production Outpost capture, queue progress, a completed spawn, full cancellation refund, rally set and clear, synchronous pause gating, resume, and menu recovery. No Pages-origin warning or error was observed; browser-extension metadata errors were excluded. Deterministic tests separately cover contest/unwind/switch, blocked-complete recovery, invalid rally preservation, destruction settlement, spawned rally movement, snapshots, replay, and checksums. Tablet/phone landscape, portrait/rotation, physical touch, broad browsers, 200% zoom, fullscreen recovery, a live blocked-spawn recovery journey, generalized stale-cache/rollback, and physical-device observations remain debt. Phase 4 closed under the owner's standing goal-mode authorization; no tag or GitHub Release was created.

### v2026.8.22e Phase 5 implementation and closure evidence

The local candidate implements the frozen nine-command protocol and deterministic combat/tactical state on the separate `phase5/` route. It includes health, role-specific attacks, sticky acquisition, attack-move resume, point/entity defend, STOP cancellation, 24-slot reservations, 96-projectile bounds, simultaneous packets, population-safe defeat, structure damage/destruction and refunds, headquarters victory/draw resolution, canonical replay/snapshot/checksum state, accessible combat controls and feedback, and simulation-owned action/defeat timing. Strategic AI, modes, campaign, networking, external requests, tags, and Releases remain absent.

The four focused Phase 5 suites pass 57/57 and the complete dependency-free suite passes 225/225. The structure-damage exporter reproducibly derives twelve exact-RGBA lossless WebPs totaling 1,040,292 bytes from eight repository-only transparent sources; decoded-source arithmetic is 6,418,944 bytes and retained two-player structure residency is 12,811,776 bytes. The manifest records twelve retained state bases, twelve prepared ownership sheets, zero border-alpha pixels, zero mask escapes, zero transparent-RGB pixels, and zero round-trip differences. Exactly 154 public files are allowlisted, plus `.nojekyll` for 155 staged files total; the Phase 5 route contributes twelve source/shell/manifest files and twelve WebPs, while all eight source PNGs remain excluded.

Protected pull request `#26` passed audit run `32582567751` and squash-merged as `4c94369888911e7ff9c06ca53836fb6903b8e304`. Main Offline audit `32582619287` and Pages deployment `32582619288` succeeded. The remote merge tree exactly matched the frozen local tree after the protected audit caught and forced repair of eight truncated large-blob uploads. Cache-busted HTTP requests matched the root and every one of the 24 Phase 5 route files byte for byte.

A visible cloud-Chrome session at 1363×936 loaded Standard and Compact art, all six layers, all representatives, structures, ownership cues, and damage resources with no Pages-origin warning or error. Named interaction evidence covered selection, focus attack, attack-move, point and entity defend, STOP, ranged combat, casualties, survivor capture, headquarters damage, production, cancellation refund, pause/resume, navigation, settings, and menu return. Tests separately own the exact destroyed/victory/draw, after-contact cancellation, reservation, restore, and replay edge cases that were not forced through the unprivileged live UI. Phase 5 closed under the owner's standing goal-mode authorization; physical-device and unavailable browser rows remain Phase 7 debt. No tag or GitHub Release was created.

### v2026.8.22f Phase 6 contract candidate

[`PHASE6_STRATEGIC_AI.md`](PHASE6_STRATEGIC_AI.md) freezes one two-player local Moonfall Crossing skirmish, one deterministic no-cheating Standard computer opponent, exact observation redaction, three bounded task forces, production/capture/retreat/assault rules, legal-command orchestration, composite checkpoints, replay behavior, local presentation, and its evidence gate. It adds no Phase 6 runtime yet and does not start modes, campaign, product hardening, networking, a tag, or a GitHub Release.

### v2026.8.22g Phase 6 implementation and closure evidence

The closed source implements the frozen contract through exactly six `phase6/` route files. `config.js` extends Phase 5 tuning by reference, `ai.js` owns detached observation and pure bounded planning, `skirmish.js` alone owns legal submission, mixed replay, suspension, battle advancement, and composite checkpoints, and the retained menu/shell identifies You and Computer outside Canvas. The implementation performs no network request, retains `connect-src 'none'`, and does not change the closed Phase 5 source bytes.

Four focused Phase 6 suites pass **37/37** and the complete dependency-free suite passes **262/262**, covering the exact configuration, observation redaction and planning, force/need scenarios, legal-command orchestration, suspension, checkpoint/restore/replay convergence, shell identity, script order, CSP, and the six-file Pages boundary. The actual passive `createSkirmish` regression with seed `0x4a0e2026` ends in a computer victory at tick **3,715**, below the 12,000-tick evidence ceiling. Exact staging succeeds with 161 public files plus `.nojekyll` (162 staged files total).

Protected pull request `#28` passed audit run `32588621330` at candidate commit `ab619699fbed11dd10e4d9309e1c79f644925771` and squash-merged as `f713d58bb7a9460bce7f9e4c88cabc999e17a20f`. The merge has exact candidate tree `e2a556e56b286d1e23d1d0c6b3399db10a6b6fb4`. Main Offline audit run `32588951779` and Pages run `32588951783` succeeded. All 161 allowlisted live files matched merged source exactly; one transient HTTP 503 was retried successfully. No tag or GitHub Release was created.

A visible cloud-Chrome session at 1363×936 rendered all six canvases at 1363×936. The Standard passive journey showed first paint, production, captures, separate reserve and front behavior, the timed assault, a readable computer victory at exact tick **3,715**, pause, and menu recovery. A separate active-pressure journey showed defense, recovery, and regroup at the West Production Outpost. The Compact 96-pixel journey passed first paint, Navigation, and menu recovery. No warning or error from the Pages origin was observed; browser-extension metadata errors were excluded. Phase 6 therefore closed under the owner's standing goal-mode authorization. Physical-device and broad-browser observations remain Phase 7 debt rather than fabricated closure evidence.

### v2026.8.22j Phase 7 publication evidence and Pages artifact correction

The published candidate adds exactly four files under `phase7/` and reuses the byte-locked Phase 2 through Phase 6 owners. It changes no authoritative combat, economy, movement, production, objective, AI, replay, checkpoint, or checksum rule. The direct route adds semantic menu, settings, command, queue, target, ownership, health, pause, outcome, and recovery state outside Canvas; safe-area and reflow behavior; composed lifecycle suspension; reduced-motion presentation; guarded fullscreen/orientation settlement; deterministic teardown; and bounded source/canvas memory arithmetic.

The focused dependency-free Phase 7 suite passes **19/19**, and the complete dependency-free suite passes **281/281** on the same candidate. Evidence includes exact closed-source locks, 12,000 active authoritative ticks, the exact 8,192-command replay boundary, reachable cap tests, checkpoint convergence, ownership-hue suppression, repeated input lifecycle teardown, the complete fullscreen/orientation settlement matrix, truthful partial release, and a bounded Node VM journey that executes the real `phase7/app.js` through keyboard-generated Settings/fullscreen/Begin/structure/production/camera/navigation/pause/cancellation/Menu activation, Escape reset, three Begin→Menu cycles, BFCache, portrait and size gates, visibility, blur/focus, reduced motion, failed preload recovery, completed-match retirement, and page teardown. The VM record is not decoded-image, Canvas-pixel, actual CSS layout, native Tab/Shift+Tab/dialog trapping, real fullscreen, physical-device, or thermal evidence. Exact Pages staging contains **166 public files plus `.nojekyll` (167 staged files total)** and adds only the four local Phase 7 route files.

Pull request `#30` passed audit `32598168338` and squash-merged as `461c100eb48d83d2ba960a300313c19848d89f41`; main audit `32598490594` and Pages run `32598490580` succeeded. Artifact `9482284342` has digest `sha256:ec946b392f45e6f33bc979c34bef3a15373278cc43744b757641fb1193a29a08` and contains all 166 public files byte-identical to the staged tree. It omitted only the empty staged `.nojekyll` because the upload action's hidden-file input defaulted off; `v2026.8.22j` enables that input only for the freshly recreated explicit `_site` boundary and locks the configuration in automation.

A visible cloud-Chrome session at CSS viewport **1363×936**, device-pixel ratio **1**, rendered six 1363×936 Canvas backing stores for both Standard and Compact art. The menu had zero horizontal overflow; every enabled authored control measured at least 44 CSS pixels high; native Tab/Shift+Tab order was logical; Settings contained focus within the native dialog and Escape returned focus. Standard play showed full-body entities, the layered environment, all three structure categories, color-plus-symbol ownership, readable HUD/health state, keyboard production and cancellation with full refund/focus recovery, tick-exact pause, Navigation, pointer selection, keyboard Move arming/Escape, terrain movement, safe fullscreen-denial recovery, and Menu teardown with all Canvas backing stores zeroed. Compact play loaded the 96-pixel tier and retained readable full-body art. No Pages-origin warning or error was observed; browser-extension diagnostics were excluded.

This is published and available-browser evidence, not Phase 7 closure. Corrected 167-file artifact evidence, direct HTTP live-byte identity, unavailable physical-device and browser-family rows, actual 200% browser zoom, browser/GPU memory and thermal evidence, all material owner decisions, and root promotion remain pending. No Phase 8, Phase 9, networking, audio, storage, tag, or GitHub Release is authorized by this candidate.

### Owner-supplied portrait mobile observation

On 2026-08-15 the product owner supplied a 945×2048 portrait mobile-browser screenshot of the live `xenovoyage.github.io` status page. The capture visibly shows the Aeon of Kingdoms wordmark, redesign status, Phase 0 label, rebuilding statement, explanatory copy, and current-boundary section. The content is readable, remains inside the visible browser safe area, and does not show the rejected gameplay shell. The screenshot does not prove the unseen remainder of the page, link activation, landscape behavior, another device or viewport, console state, offline reopening, or migration from an existing prototype service-worker cache.

## Next boundary

1. Publish this bounded artifact correction through the protected path, download the resulting Pages artifact, and prove all 166 public files plus the empty `.nojekyll` match the merged stage exactly.
2. Keep direct HTTP live-byte identity pending unless an allowed inspection surface can observe it; the exact deployment artifact and named rendered page are separate evidence categories.
3. Carry unavailable tablet/phone landscape, portrait, touch, broad-browser, keyboard-only spatial control, actual 200% zoom, browser/GPU memory, thermal, and physical-device observations as explicit debt rather than inferred results.
4. Keep audio, onboarding, persistence, a keyboard-only spatial model, named physical-device coverage, thermal thresholds, and root promotion as explicit material owner gates. The root remains the status page until separately approved.
5. Keep the private host/client room-code plan documentation-only until Phase 9; do not start Phase 8 or Phase 9 runtime while Phase 7 remains open, and do not create a tag or GitHub Release.
